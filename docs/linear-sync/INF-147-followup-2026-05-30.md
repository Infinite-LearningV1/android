# Linear Sync Drafts — 2026-05-30

## INF-147 — Android refresh/session compatibility

Draft comment:

Android consumer alignment for INF-145 is implemented on branch `fix/inf-147-login-refresh-token-compat`.

Completed locally:

- Login parses JSON `refresh_token` for mobile consumer.
- `ApiService.refresh()` targets `POST /api/auth/refresh`.
- `X-Client-Type: mobile` is attached by the auth interceptor.
- Refresh token is persisted in `UserPreference` with `lastRefreshAt`.
- Repository refresh maps backend codes to typed failure kind/reason.
- Single-flight refresh coordinator reuses one refresh attempt for concurrent callers.
- Interceptor refreshes/replays on `AUTH_ACCESS_TOKEN_EXPIRED` and forces reauth on invalid/revoked/inactive session codes.
- Bootstrap avoids false logout on refresh transport failure when cached session exists.
- Login banner messages are mapped from forced reauth reasons.
- Logout sends refresh-token body when available for backend revoke fallback.

Evidence:

- Unit tests added/updated for contract parsing, preference persistence, repository refresh/logout, coordinator, interceptor, session manager, bootstrap, and login banner.
- Runtime smoke evidence template: `docs/auth-runtime-evidence/RUN_2026-05-30.md`.

Pending:

- Fill runtime smoke evidence after running against the live INF-145 backend.

## INF-145 — Backend auth contract heads-up

Draft comment:

Android has aligned to the INF-145 mobile contract in `fix/inf-147-login-refresh-token-compat`:

- uses `X-Client-Type: mobile`
- consumes JSON `refresh_token`
- calls `POST /api/auth/refresh`
- handles `AUTH_ACCESS_TOKEN_EXPIRED`, `AUTH_REFRESH_TOKEN_INVALID`, `AUTH_REFRESH_TOKEN_REVOKED`, and `AUTH_SESSION_INACTIVE`
- supports logout revoke fallback via refresh-token body

Runtime evidence will be attached after Android smoke verification.

## INF-148 — auth_sessions cleanup dependency

Draft comment:

Android now depends on backend session semantics for mobile refresh/revoke. Please keep INF-148 cleanup coordinated with Android cutover so stale/legacy `auth_sessions` records do not create confusing forced reauth during testing.

## INF-149 — legacy access cutover dependency

Draft comment:

Android branch now assumes mobile sessions use refresh-token based continuity and explicit backend auth codes. During INF-149 legacy access cutover, please preserve clear error codes for clients without valid `session_id` so Android can distinguish forced reauth vs temporary transport/server failure.
